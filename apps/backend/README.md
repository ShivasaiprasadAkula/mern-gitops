# Backend - Chat App
Setup: copy `.env.example` to `.env`, install dependencies, and run.

## Quickstart
```
cd backend
npm install
cp .env.example .env
npm run dev
```

API endpoints listed in code, runs on `PORT` from `.env`.
