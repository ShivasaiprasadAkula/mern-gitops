apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chat-frontend
spec:
  rules:
    - host: chat.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: chat-frontend
                port:
                  number: 80
